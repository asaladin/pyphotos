[app:myapp]
use = egg:pyphotos

pyramid.reload_templates = true
pyramid.debug_authorization = false
pyramid.debug_notfound = false
pyramid.debug_routematch = false
pyramid.debug_templates = true
pyramid.default_locale_name = en
pyramid.includes = 
       pyramid_debugtoolbar
       pyramid_tm

sqlalchemy.url = sqlite:///%(here)s/pyphotos.sqlite

db_uri = mongodb://localhost/
mongo.url = mongodb://localhost/
db_name = pyphotos
mongo.database = pyphotos

allownewaccount = True
admin_email = adrien.saladin@gmail.com
persona.siteid = http://pyphotos:6543

session.type = file
session.expire = 600
session.data_dir = %(here)s/data/sessions/data
session.lock_dir = %(here)s/data/sessions/lock
session.key = customerskey
session.secret = customerssecret
session.cookie_on_exception = true

bucket_name = asphotos
pyphotos_debug_mode = true

[filter:paste_prefix]
use = egg:PasteDeploy#prefix

[pipeline:main]
pipeline = 
     paste_prefix
     myapp



[server:main]
use = egg:waitress#main
host = 0.0.0.0
port = 6543

# Begin logging configuration

[loggers]
keys = root, pyphotos

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = INFO
handlers = console

[logger_pyphotos]
level = DEBUG
handlers =
qualname = pyphotos

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(asctime)s %(levelname)-5.5s [%(name)s][%(threadName)s] %(message)s

# End logging configuration
